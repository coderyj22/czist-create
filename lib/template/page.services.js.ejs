/**
 * <%= data.lowerName%> services
 */
import request from "@/utils/request";
import { <%= data.lowerName%>Api } from '@/utils/ApiPath';

export async function GetList(data) {
  return request(<%= data.lowerName%>Api.GetList, {
    method: 'POST',
    data,
  });
}

export async function AddEntity(data) {
  return request(<%= data.lowerName%>Api.AddEntity, {
    method: 'POST',
    data,
  });
}

export async function UpdateSave(data) {
  return request(<%= data.lowerName%>Api.UpdateSave, {
    method: 'POST',
    data,
  });
}

export async function SetDisable(data) {
  return request(<%= data.lowerName%>Api.SetDisable, {
    method: 'POST',
    data,
  });
}

export async function SetEnable(data) {
  return request(<%= data.lowerName%>Api.SetEnable, {
    method: 'POST',
    data,
  });
}

export async function GetEntity(data) {
  return request(<%= data.lowerName%>Api.GetEntity, {
    method: 'POST',
    data,
  });
}

export async function Delete(data) {
  return request(<%= data.lowerName%>Api.Delete, {
    method: 'POST',
    data,
  });
}
